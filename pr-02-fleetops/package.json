{
  "name": "fleetops",
  "version": "1.0.0",
  "description": "FleetOps is a **conceptual frontend-only SaaS dashboard** for transport and fleet operations management. The project is part of a larger professional portfolio and focuses on **application architecture, UI state management, and SaaS-style navigation patterns**, rather than backend implementation.",
  "main": "sw.js",
  "directories": {
    "doc": "doc"
  },
  "scripts": {
    "build": "node build-dist.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "min:css": "postcss \"styles/!(*.min).css\" --dir styles --ext .min.css",
    "min:js": "node minify-js.js",
    "min:all": "npm run min:css && npm run min:js",
    "preview:dist": "cd dist && python -m http.server 8182"
  },
  "keywords": [],
  "author": "KP_Code Digital Studio",
  "license": "UNLICENSED",
  "devDependencies": {
    "cssnano": "^7.1.2",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1",
    "terser": "^5.44.1"
  }
}
